# Generated by Okty.io
version: '3.6'
services:
    php-fpm:
        build: docker/php
        volumes:
            - './:/usr/share/nginx/html'
    mariadb:
        image: 'mariadb:10.3'
        environment:
            - MYSQL_DATABASE=mydb
            - MYSQL_USER=valentin
            - MYSQL_PASSWORD=valentin
            - MYSQL_RANDOM_ROOT_PASSWORD=yes
    nginx:
        build: docker/nginx
        ports:
            - '80:80'
        volumes:
            - './:/usr/share/nginx/html'
